'use client'

import { FormEvent, useState } from 'react'
import { useRouter } from 'next/navigation'
import { Eye, EyeOff, LockKeyhole, Mail, UtensilsCrossed, UserRound } from 'lucide-react'
import { authClient } from '@/lib/auth-client'

type Mode = 'restaurant' | 'admin'

export function RestaurantAuthForm() {
  const router = useRouter(); const [mode, setMode] = useState<Mode>('restaurant'); const [create, setCreate] = useState(false); const [name, setName] = useState(''); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [show, setShow] = useState(false); const [error, setError] = useState(''); const [pending, setPending] = useState(false)
  async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setError(''); setPending(true); const result = create ? await authClient.signUp.email({ email, password, name: name.trim() || 'Administrador' }) : await authClient.signIn.email({ email, password }); if (result.error) { setError('No pudimos completar el acceso. Verifica tus datos e inténtalo de nuevo.'); setPending(false); return } router.push(mode === 'admin' ? '/' : '/restaurante'); router.refresh() }
  return <div className="w-full max-w-md rounded-2xl border border-border bg-card p-6 shadow-xl shadow-foreground/5 sm:p-8"><div className="mb-7 flex items-center gap-3"><div className="flex size-11 items-center justify-center rounded-xl bg-primary text-primary-foreground"><UtensilsCrossed size={20}/></div><div><p className="font-semibold">Mesa<span className="text-primary">Flow</span></p><p className="text-xs text-muted-foreground">Acceso seguro</p></div></div><div className="mb-6"><p className="mb-2 text-sm font-medium text-primary">Selecciona tu espacio</p><div className="grid grid-cols-2 gap-2 rounded-lg bg-muted p-1"><button type="button" onClick={()=>setMode('restaurant')} className={`rounded-md px-3 py-2 text-sm ${mode==='restaurant'?'bg-card font-medium shadow-sm':''}`}>Restaurante</button><button type="button" onClick={()=>setMode('admin')} className={`rounded-md px-3 py-2 text-sm ${mode==='admin'?'bg-card font-medium shadow-sm':''}`}>Administrador</button></div></div><h1 className="text-2xl font-semibold tracking-tight">{create?'Crea tu espacio':'Bienvenido de vuelta'}</h1><p className="mt-2 text-sm leading-6 text-muted-foreground">{mode==='admin'?'Gestiona todos tus restaurantes y sucursales.':'Opera ventas, carta, inventario y facturación.'}</p><form onSubmit={submit} className="mt-6 flex flex-col gap-4">{create&&<label className="flex flex-col gap-2 text-sm font-medium">Tu nombre<div className="relative"><UserRound size={16} className="absolute left-3 top-1/2 -translate-y-1/2 text-muted-foreground"/><input required value={name} onChange={e=>setName(e.target.value)} className="h-11 w-full rounded-lg border border-input bg-background pl-10 pr-3" placeholder="Nombre del responsable"/></div></label>}<label className="flex flex-col gap-2 text-sm font-medium">Correo electrónico<div className="relative"><Mail size={16} className="absolute left-3 top-1/2 -translate-y-1/2 text-muted-foreground"/><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="h-11 w-full rounded-lg border border-input bg-background pl-10 pr-3" placeholder="equipo@restaurante.com"/></div></label><label className="flex flex-col gap-2 text-sm font-medium">Contraseña<div className="relative"><LockKeyhole size={16} className="absolute left-3 top-1/2 -translate-y-1/2 text-muted-foreground"/><input required minLength={8} type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} className="h-11 w-full rounded-lg border border-input bg-background pl-10 pr-11" placeholder="Mínimo 8 caracteres"/><button type="button" aria-label="Mostrar contraseña" onClick={()=>setShow(!show)} className="absolute right-3 top-1/2 -translate-y-1/2"><Eye size={16}/></button></div></label>{error&&<p role="alert" className="rounded-lg bg-accent px-3 py-2 text-sm text-accent-foreground">{error}</p>}<button disabled={pending} className="h-11 rounded-lg bg-primary text-sm font-medium text-primary-foreground disabled:opacity-60">{pending?'Procesando…':create?'Crear espacio y entrar':`Entrar como ${mode==='admin'?'administrador':'restaurante'}`}</button></form><button type="button" onClick={()=>setCreate(!create)} className="mt-5 w-full text-center text-sm text-primary hover:underline">{create?'Ya tengo una cuenta':'Crear una cuenta nueva'}</button></div>
}
